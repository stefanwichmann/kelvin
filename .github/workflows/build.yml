name: build

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v6
      with:
        fetch-depth: 0

    - name: Set up Go
      uses: actions/setup-go@v6
      with:
        go-version: 'stable' # Latest stable version

    - name: Build
      run: go build -v ./...

    - name: Test
      run: go test -v ./...

  docker-smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
    - uses: actions/checkout@v6

    - name: Set up Go
      uses: actions/setup-go@v6
      with:
        go-version: 'stable' # Latest stable version

    - name: Docker distribution smoke test
      run: ./scripts/docker-smoke.sh
